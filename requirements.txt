# Runtime + test dependencies. Install with:  pip install -r requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.27
httpx>=0.27          # used only in LIVE mode (and by the test client)
pytest>=8.0          # for the test suite
